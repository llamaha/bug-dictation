# Audio capture (uses system portaudio, already installed)
sounddevice
numpy
soundfile

# Speech-to-text
openai-whisper

# Talking to ollama
requests

# torch is installed separately from the CUDA 12.8 index (Blackwell / RTX 5090):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
